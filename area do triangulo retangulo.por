programa {
  funcao inicio() {
    inteiro base
    inteiro altura
    inteiro resultado

    escreva("digite a base: ")
    leia(base)

    escreva("digite a altura: ")
    leia(altura)

    resultado = (base * altura) / 2

    escreva("o resultado da area do triangulo é: ",resultado)
  }
}
